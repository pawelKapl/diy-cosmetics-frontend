import { IngredientQuantity } from './ingredient-quantity';

describe('IngredientQuantity', () => {
  it('should create an instance', () => {
    expect(new IngredientQuantity()).toBeTruthy();
  });
});
